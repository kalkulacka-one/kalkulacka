// import { useQuestionsStore } from "../../providers/storeProvider";
// import type Store from "../../providers/storeProvider";

// // better approach to subscribe to a store
// const stateSelector = (state: Store) => ({
//   guide: state.guide,
//   answerYes: state.answerYes,
// });

// const { guide, answerYes } = useQuestionsStore(stateSelector);

// export default function Some() {
//   return <h1>Some</h1>;
// }
