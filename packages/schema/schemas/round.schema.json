{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.kalkulacka.one/round.schema.json",
  "title": "Round",
  "description": "Round of an election",
  "type": "object",
  "properties": {
    "key": {
      "title": "Key",
      "description": "Human-friendly unique key of a round in the hyphen-separated lowercased format",
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "examples": ["2-kolo", "2nd-round"]
    },
    "number": {
      "title": "Number",
      "description": "Round ordinal number from 1",
      "type": "integer",
      "minimum": 1,
      "examples": [1, 2]
    },
    "votingHours": {
      "title": "Voting hours",
      "description": "One or multiple voting hours for the round",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "./time-period.schema.json"
      }
    }
  },
  "unevaluatedProperties": false,
  "required": ["key", "number"],
  "$defs": {
    "reference": {
      "title": "Round reference",
      "description": "Reference to a round of an election",
      "type": "object",
      "properties": {
        "key": {
          "$ref": "#/properties/key"
        }
      },
      "unevaluatedProperties": false,
      "required": ["key"]
    }
  }
}
